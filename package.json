{
  "name": "project-universe-fep-epi-static",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare static build for the FEP/EPI trainer.",
  "scripts": {
    "build": "node build-static.js",
    "validate:english": "npm run build && node validate-english-test.js",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "wrangler": "^4.103.0"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@simplewebauthn/server": "^13.3.2",
    "jose": "^6.2.3"
  }
}
